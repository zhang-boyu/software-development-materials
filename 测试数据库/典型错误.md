# 理论

## 填空

### 第5题
训练、测试、预测/推理（张明鑫、殷浩）

### 第6题、第7题
训练过程：基于 **损失函数** 计算 **损失**，然后调整**模型参数**使损失减小（张明鑫、殷浩）

### 第10题
离散和连续的概念（张明鑫）


### 第11题
术语：离散（金欣悦）

## 判断题
### 第4题、第5题、第6题
图


## 应用题

### 第1题：

（1）特征一般是对结果（标签）有影响的，所以学生姓名、编号、id之类的不作为特征

（2）术语：标注（金欣悦）
		
		
		
### 第4题
C：强化学习，介绍概念


### 第5题
（3）模型训练的详细过程：
- 第1步：随机初始化a、b、c，得到初始化模型
- 第2步：将训练集中的面积和房龄输入该公式，得到模型预测结果
- 第3步：计算模型预测结果与实际结果的误差，即损失
- 第4步：基于特定的算法定向调整a、b、c（有特定算法可以根据损失计算如何调整a、b、c），得到更新后的模型
- 第5步：循环第2步~第4步，直到损失不再下降（专业术语：损失收敛）



# 编程：
## 整体问题：
### 1）务必按照题目要求提交文件（25级同学）
一般要提交的：
- .py文件（代码不要复制到txt，直接提交.py代码，一道题目提交一个.py）
- jpg截图（一般是中间的print输出结果）
- md说明文档（后续再教）
- 其他题目要求文件
### 2）调试时注释掉的有用的代码，务必取消注释（张家源table_process.py，金欣悦table_process.py）

### 3）审题，按照题目要求完成（张家源table_process.py，脱敏部分）

### 4）字符串格式的文件路径，如'E:\images\1.jpg'，前面加r，即r'E:\images\1.jpg'（黄文杰）

### 5）函数名称拼写、记忆错误（谢如梦map，一行两个=，哈希.hexdigest()、大小写、正则的引号位置等；黄文杰map函数用法，金欣悦sha256，df['id_card']）



## 1、环境题
- 未完成（杨姝瑜）

- pycharm配置环境

- python版本
```
import sys
print(sys.version)
```
- 库的版本
```
import 库名
print(库名.__version__)
如：
import cv2
print(cv2.__version__)
```


## 2、openCV
- 未提交完整代码（杨姝瑜）

- 读取图片
```
cv2.imread(图片路径)
这里图片路径包括：所在路径（C盘开始一直到图片所在文件夹）+图片名称（需要有.jpg这样的后缀）
```


- 获取文件夹里所有图片，遍历图片，在循环里挨个读取并处理图片（谢如梦未在循环处理，黄文杰未获取图片，金欣悦未获取图片）
```
获取所有图片
filelist = os.listdir(r'E:\python Project\jinengdasai2027\practice\202607-data_preprocess\images')
遍历图片
for filename in filelist:
    挨个读取图片（在循环里！）
    image = cv2.imread(os.path.join(r'E:\python Project\jinengdasai2027\practice\202607-data_preprocess\images', filename))
    挨个处理图片（在循环里！）
    resized = cv2.resize(image, (300, 400))
    ...
```


- 获取图片原始名称（张明鑫）
```
上面循环里的filename是包括后缀名的图片名称，如'1.jpg'
如果直接在filename后面加'_resized.jpg'，会变成'1.jpg_resized.jpg'，很奇怪
所以需要先获取无后缀名的图片名称，即获取'1'：
new_name = os.path.splitext(filename)[0]
然后在new_name后面拼接'_resized.jpg'
```



## 3、pandas表格题
- 未完成（张明鑫，杨姝瑜未完成IQR和保存，黄文杰未完成IQR和哈希）
- 删除“南京市”需要用空字符串替代（金欣悦）
```
def nanjing(x):
    return x.replace("南京市","")   这里是""，不是" "
```



## 4、nlp题目
- 未完成（张明鑫、殷浩、杨姝瑜、谢如梦、黄文杰，金欣悦）

- 导入库和函数是固定用法，不要和变量名称混淆（谢如梦）

- 清洗
```
单条文本清洗：（黄文杰，金欣悦）
text = re.sub(r'[^\u4e00-\u9fa5]', '', text)     text是字符串
整列文本清洗：
texts = texts.str.replace(r'[^\u4e00-\u9fa5]', '', regex=True)     texts是pandas表格的一列
```

- 去停用词是，停用词表建议用set集合，可以加速（张家源）
```
with open("stopwords.txt", encoding = "utf-8") as f:
    stopwords = set([w.strip() for w in f.readlines()])
```

- 文字向量化（张家源）

  - 第1步：选择编码器 
  ```
  from sklearn.feature_extraction.text import TfidfVectorizer
  或
  from sklearn.feature_extraction.text import CountVectorizer
  ```
  - 第2步：编码器实例化
  ```
  vec = TfidfVectorizer()
  或
  vec = CountVectorizer()
  ```
  - 第3步：使用编码器完成文字向量化
  ```
  texts = vec.fit_transform(df["Text"])
  前提：df["Text"]的每一条数据是已经完成分词并用空格拼接的字符串，如：'我 来自 无锡'
  ```
  - 额外步骤：输出结果
  ```
  输出词表：
  print(vec.get_feature_names_out())
  输出词表大小：
  print(len(vec.get_feature_names_out()))
  输出矩阵形状：
  print(texts.shape)
  ```







