# 无锡职业技术学院内部资料

JAVA程序设计

# *

任务描述

知识链接

任务实现

目录

# *

模块一  任务描述

如果按照学生成绩的范围（满分100分的情况下），对于各范围成绩给出等级划分即成绩大于等于90分输出优秀，成绩大于等于80分输出良好，成绩大于等于70分输出中等，成绩大于等于60分输出及格，成绩小于60分输出不及格。

# ..

模块二 知识链接

if...else...if...else语句语法格式

if...else...if...else语句流程图

# if...else if...else语句

if (判断条件1){
执行语句1
}
else if (判断条件2){
执行语句2
}
……
else if(判断条件n-1){
语句n-1
}
else{
执行语句n
}

语法格式

模块二 
知识链接

# if...else if...else语句

执行流程

模块二 
知识链接

图3-5 if...else if...else语句执行流程

# ，

过渡页

模块三  任务实现

如果按照学生成绩的范围（满分100分的情况下），对于各范围成绩给出等级划分即成绩大于等于90分输出优秀，成绩大于等于80分输出良好，成绩大于等于70分输出中等，成绩大于等于60分输出及格，成绩小于60分输出不及格。

模块三 
任务实现

# *

过渡页

从键盘输入成绩

判断等级

模块三  任务实现

输出结果

# 任务源码

结合任务3-3中的描述和知识链接中if...else if...else语句知识点可以得到文件3-3所示的代码：

模块三 
任务实现

文件3-3 Example03.Java

# 输出结果

模块三 
任务实现

运行结果如图3-6所示：

图3-6文件3-3的运行结果

# 感谢关注