# 无锡职业技术学院内部资料

JAVA程序设计

# *

任务描述

知识链接

任务实现

目录

# *

模块一  任务描述

子任务1：定义byte类型的变量num为int类型的i赋值。
子任务2：定义int类型的变量num为byte类型的b赋值。
子任务3：定义byte类型的变量和int类型的变量，计算两个变量      的和，并将和的值赋值给byte类型的变量。
子任务4：定义byte类型的变量和byte类型的变量，计算两个变量的和，并将和的值赋值给byte类型的变量。

# *

模块二 知识链接

自动类型转换

强制类型转换

表达式类型的自动提升

# 自动类型转换

模块二 
知识链接

自动类型转换也叫隐式类型转换，指的是两种数据类型在转换的过程中不需要显式地进行声明，由编译器自动完成。自动类型转换必须同时满足以下两个条件：

（1）两种数据类型彼此兼容。

（2）目标类型的取值范围大于源类型的取值范围。

# 模块二 
知识链接

使用byte类型的变量b为int类型的变量x赋值，由于int类型的取值范围大于byte类型的取值范围，编译器在赋值过程中不会造成数据丢失，所以编译器能够自动完成这种转换，在编译时不报告任何错误。

自动类型转换举例

byte b = 3;
int x = b;

# 模块二 
知识链接

很多类型之间可以进行自动类型转换。下面列出3种可以进行自动类型转换的情况，具体如下。

（1）整数类型之间可以实现转换。
例如，byte类型的数据可以赋值给short、int、long类型的变量；short、char类型的数据可以赋值给int、long类型的变量；int类型的数据可以赋值给long类型的变量。
（2）整数类型转换为float类型。
例如，byte、char、short、int类型的数据可以赋值给float类型的变量。
（3）其他类型转换为double类型。
例如，byte、char、short、int、long、float类型的数据可以赋值给double类型的变量。

自动类型转换

# 强制类型转换

模块二 
知识链接

强制类型转换也叫显式类型转换，指的是两种数据类型之间的转换需要进行显式地声明。当两种类型彼此不兼容，或者目标类型取值范围小于源类型时，自动类型转换无法进行，这时就需要进行强制类型转换。

# 强制类型转换

模块二 
知识链接

在学习强制类型转换之前，先来看个例子，在本例中，使用int类型的变量num为byte类型的b赋值，具体代码如下所示。

public class Example01 {
	public static void main(String[] args) {
		int num = 4;
		byte b = num;
		System.out.println(b);
	}
}

# 强制类型转换

模块二 
知识链接

运行代码，控制台显示的运行结果如下图所示。

# 强制类型转换

模块二 
知识链接

运行结果分析

由上图可知，程序提示数据类型不兼容，不能将int类型转换成byte类型，原因是将一个int类型的值赋给byte类型的变量b时，由于int类型的取值范围大于byte类型的取值范围，这样的赋值会导致数值溢出，也就是说1字节的变量无法存储4字节的整数值。

# 强制类型转换

模块二 
知识链接

使用强制转换

针对上述情况，就需要进行强制类型转换，即强制将int类型的值赋值给byte类型的变量。强制类型转换格式如下：

目标类型  变量 = （目标类型）值

将上述案例一中的第4行代码修改为下面的代码。

byte b = (byte) num;

# 强制类型转换

模块二 
知识链接

修改后运行结果

修改后运行代码，控制台显示的运行结果如下图所示。

注意：在对变量进行强制类型转换时，如果将取值范围较大的数据类型强制转换为取值范
围较小的数据类型，如将一个int类型的数转为byte类型，极容易造成数据精度的丢失。

# 强制类型转换

模块二 
知识链接

public class Example02 {
	public static void main(String[] args) {
		byte a;                // 定义byte类型的变量a
		int b = 298;          // 定义int类型的变量b
		a = (byte) b;
		System.out.println("b=" + b);
		System.out.println("a=" + a);
	}
}

下面通过一个案例演示数据精度丢失的情况，具体代码如下所示。

# 强制类型转换

模块二 
知识链接

运行结果

运行代码，控制台显示的运行结果如下图所示。

# 强制类型转换

模块二 
知识链接

案例二运行结果分析

变量b本身的值为298，然而在赋值给变量a后，a的值为42。出现这种现象的原因是，变量b为int类型，在内存中占用4个字节；byte类型的数据在内存中占用1个字节，当将变量b的类型强转为byte类型后，前面3个高位字节的数据丢失，数值发生改变。

# 强制类型转换

模块二 
知识链接

int类型转换为byte类型的过程

int类型转换为byte类型的过程如下图所示。

# 表达式类型自动提升

模块二 
知识链接

所谓表达式是指由变量和运算符组成的一个算式。变量在表达式中进行运算时，可能发生自动类型转换，这就是表达式数据类型的自动提升。例如，一个byte类型的变量在运算期间类型会自动提升为int类型。

# 表达式类型自动提升

模块二 
知识链接

所谓表达式是指由变量和运算符组成的一个算式。变量在表达式中进行运算时，可能发生自动类型转换，这就是表达式数据类型的自动提升。例如，一个byte类型的变量在运算期间类型会自动提升为int类型。

# *

过渡页

模块三  任务实现

子任务1：定义byte类型的变量num为int类型的i赋值。
子任务2：定义int类型的变量num为byte类型的b赋值。
子任务3：定义byte类型的变量和int类型的变量，计算两个变量      的和，并将和的值赋值给byte类型的变量。
子任务4：定义byte类型的变量和byte类型的变量，计算两个变量的和，并将和的值赋值给byte类型的变量。

# 子任务1-代码实现

1  public class Example05 {
2 	public static void main(String[] args) {
3		byte num = 4;
4		int i =num;
5		System.out.println("i="+i);
6	}
7  }

模块三 
任务实现

# 运行结果

模块三 
任务实现

# 子任务2-代码实现

1  public class Example06 {
2	public static void main(String[] args) {
3		int num = 4;
4		byte b =(byte) num;
5		System.out.println("b="+b);
6	}
7  }

模块三 
任务实现

# 运行结果

模块三 
任务实现

# 子任务3-代码实现

1  public class Example07 {
2	public static void main(String[] args) {
3	       byte b1 = 3;           // 定义一个byte类型的变量
4	       int i = 4;           // 定义一个int类型的变量
5	       byte b2 = (byte)(b1 + i);// 两个变量相加，赋值给一个byte类型变量
6	       System.out.println("b2=" + b2);
7	}
8  }

模块三 
任务实现

# 运行结果

模块三 
任务实现

# 子任务4-代码实现

1  public class Example08 {
2	public static void main(String[] args) {
3	        byte b1 = 3;           // 定义一个byte类型的变量
4	        byte b2 = 4;          // 定义一个byte类型的变量
5	        byte b3 =(byte) (b1 + b2);// 两个byte类型变量相加，赋值给一个byte类型变量
6	        System.out.println("b3=" + b3);
7	}
8  }

模块三 
任务实现

# 运行结果

模块三 
任务实现

# 感谢关注