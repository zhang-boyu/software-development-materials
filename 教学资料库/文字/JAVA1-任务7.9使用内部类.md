# 无锡职业技术学院内部资料

JAVA程序设计项目教程

# *

任务描述

知识链接

任务实现

目录

# 子任务2：在子任务1的基础上，定义子类Cat的属性color以及设置和获取方法；定义测试类输出Cat实例相关属性信息。

模块一  任务描述

子任务1：测试成员内部类的使用：定义外部类Pet并在其中定义属性hobby和成员方法void showPet()方法；在Pet类中定义内部类Cat并在其中定义 void showCat()方法，在该方法中访问外部类属性hobby和外部类方法showPet()；定义测试类,创建外部类实例和内部类实例并分别访问其成员方法showPet()方法和showCat()。

# 子任务2：在子任务1的基础上，定义子类Cat的属性color以及设置和获取方法；定义测试类输出Cat实例相关属性信息。

模块一  任务描述

子任务2：测试局部内部类的使用：定义外部类Pet并在其中定义属性hobby和成员方法void showPet()、void showCat()方法，在showCat()方法中定义内部类Cat,Cat中包含成员变量age和void show()方法，在show()方法中访问外部类变量hobby和方法showPet()；在showCat()方法中定义局部类Cat实例并访问访问局部内部类中的变量age和方法show();定义测试类, 创建外部类实例并调用其方法showCat()。

# 子任务2：在子任务1的基础上，定义子类Cat的属性color以及设置和获取方法；定义测试类输出Cat实例相关属性信息。

模块一  任务描述

子任务3：静态内部类的使用:定义外部类Pet并在其中定义静态属性hobby，定义静态内部类Cat，在其中定义成员变量age和void show()方法，在show()方法中访问外部类变量hobby；定义测试类,创建内部类实例并分别访问其成员方法show()。

# 子任务2：在子任务1的基础上，定义子类Cat的属性color以及设置和获取方法；定义测试类输出Cat实例相关属性信息。

模块一  任务描述

子任务4：匿名内部类的使用：定义接口Pet，在其中定义抽象方法show()；定义测试类,在其中定义变量name，定义静态方法PetShow(Pet pet)，调用该静态方法，参数为匿名内部类。

# ..

模块二 知识链接

内部类

成员内部类

局部内部类

匿名内部类

# 内部类

模块二 
知识链接

在Java中，允许在一个类的内部定义类，这样的类称作内部类，内部类所在的类称作外部类。内部类可以访问外部类的所有成员（包括私有成员），即使这些成员在内部类被定义之后才被声明。此外，内部类可以隐藏起来，只被其外部类访问，从而提供了额外的封装性。内部类主要有四种形式，分别是：成员内部类、局部内部类、静态内部类、匿名内部类。

# 成员内部类

模块二 
知识链接

在一个类中除了可以定义成员变量、成员方法，还可以定义类，这样的类被称作成员内部类。成员内部类可以访问外部类的所有成员，无论外部类的成员是何种访问权限。如果想通过外部类访问内部类，则需要通过外部类创建内部类对象，创建内部类对象的具体语法格式如下：
外部类名 外部类对象 = new 外部类名();
外部类名.内部类名 内部类对象 = 外部类对象.new 内部类名();
Pet pet=new Pet();
Pet.Cat cat=pet.new Cat();

# 局部内部类

模块二 
知识链接

局部内部类，也称为方法内部类，是指定义在某个局部范围中的类，它和局部变量都是在方法中定义的，有效范围只限于方法内部。
在局部内部类中，局部内部类可以访问外部类的所有成员变量和成员方法，而在外部类中无法直接访问局部内部类中的变量和方法。如果要在外部类中访问局部内部类的成员，只能在局部内部类的所属方法中创建局部内部类的对象，通过对象访问局部内部类的变量和方法。

# 静态内部类

模块二 
知识链接

静态内部类就是使用static关键字修饰的成员内部类。与成员内部类相比，在形式上，静态内部类只是在内部类前增加了static关键字，但在功能上，静态内部类只能访问外部类的静态成员，通过外部类访问静态内部类成员时，因为程序已经提前在静态常量区分配好了内存，所以即使静态内部类没有加载，依然可以通过外部类直接创建一个静态内部类对象。创建静态内部类对象的基本语法格式如下：
外部类名.静态内部类名 变量名 = new 外部类名.静态内部类名();

# 匿名内部类

模块二 
知识链接

在Java中调用某个方法时，如果该方法的参数是接口类型，那么在传参时，除了可以传入一个接口实现类，还可以传入实现接口的匿名内部类作为参数，在匿名内部类中实现接口方法。匿名内部类就是没有名称的内部类，定义匿名内部类时，其类体作为new语句的一部分。定义匿名内部类的基本语法格式如下所示。
new 继承的父类或实现的接口名(){
   //匿名内部类的类体
}
内部类在Java中是一个非常强大的特性，它提供了额外的封装和灵活性，但也需要注意合理使用，以避免代码过于复杂。

# ，

过渡页

模块三  任务实现

子任务1：测试成员内部类的使用：定义外部类Pet并在其中定义属性hobby和成员方法void showPet()方法；在Pet类中定义内部类Cat并在其中定义 void showCat()方法，在该方法中访问外部类属性hobby和外部类方法showPet()；定义测试类,创建外部类实例和内部类实例并分别访问其成员方法showPet()方法和showCat()。

模块三 
任务实现

# 任务源码

子任务1：根据任务描述以及成员内部类相关知识点可以得到文件7-19所示的代码：

模块三 
任务实现

文件7-19  Example19

1public class Pet  {
2	hobby="晒太阳";
3	public String showPet(){
4		 	return "这是一只猫";
5		   }
6	public class Cat  {
7		void showCat() {  
8	     System.out.println("猫喜欢"+hobby);//在成员内部类的方法中访问外部类成员变量
9	     System.out.println(showPet());//在成员内部类的方法中访问外部类成员方法
10	    }  
11		  }
12	 		}

13public class Example19 {
14	public static void main(String[] args) {
15		Pet pet=new Pet();//创建外部类实例
16	    Pet.Cat cat=pet.new Cat();//创建内部类实例
17	 	pet.showPet();//外部类访问其成员方法
18	 	cat.showCat();//内部类访问其成员方法
19	 		}
20}

# 运行结果

模块三 
任务实现

文件7-19的运行结果如图7-21所示：

图7-21文件7-19的运行结果

# ，

过渡页

模块三  任务实现

子任务2：测试局部内部类的使用：定义外部类Pet并在其中定义属性hobby和成员方法void showPet()、void showCat()方法，在showCat()方法中定义内部类Cat,Cat中包含成员变量age和void show()方法，在show()方法中访问外部类变量hobby和方法showPet()；在showCat()方法中定义局部类Cat实例并访问访问局部内部类中的变量age和方法show();定义测试类, 创建外部类实例并调用其方法showCat()。

模块三 
任务实现

# 任务源码

根据任务描述以及局部内部类相关知识点可以得到文件7-20所示的代码：

模块三 
任务实现

文件7-20  Example20

1public class Pet  {
2	String hobby="晒太阳";  // 定义外部类的成员变量
3    //定义外部类的一个成员方法showPet()
4   public void showPet(){
5		 	 System.out.println("这是一只宠物");
6		   }
7   public void showCat() {
8        //定义一个局部内部类，在局部内部类中访问外部类变量和方法
9        class Cat {
10            int age = 3;
11            void show() {
12                System.out.println("猫咪喜欢"+hobby);
13                showPet();
14            }
15        }

16        //访问局部内部类中的变量和方法
17         Cat cat = new Cat();
18         System.out.println("猫咪今年" +cat.age+"岁了");
19         cat.show();
20    }
21	 		}
22public class Example20 {
23	public static void main(String[] args) {
24		Pet pet = new Pet();
25      pet.showCat(); //通过外部类对象pet调用创建了局部内部类的方法showCat()
26	}
27}

# 运行结果

模块三 
任务实现

文件7-20的运行结果如图7-22所示：

图7-22文件7-20的运行结果

# ，

过渡页

模块三  任务实现

子任务3：静态内部类的使用:定义外部类Pet并在其中定义静态属性hobby，定义静态内部类Cat，在其中定义成员变量age和void show()方法，在show()方法中访问外部类变量hobby；定义测试类,创建内部类实例并分别访问其成员方法show()。

模块三 
任务实现

# 任务源码

根据任务描述以及静态内部类相关知识点可以得到文件7-21所示的代码：

模块三 
任务实现

文件7-21  Example21

1class Pet {
2   static String hobby="晒太阳"; // 定义类的静态变量
    // 定义了一个静态内部类
3    static class Cat {
4        int age = 3;
5        void show() {
6            // 在静态内部类的方法中访问外部类的静态变量hobby
7            System.out.println("猫的爱好是"+hobby);
8        }
9    }
10}

11public class Example21 {
12    public static void main(String[] args) {
13        Pet.Cat cat = new Pet.Cat();//创建内部类实例
14        cat.show();
15    }
16}

# 运行结果

模块三 
任务实现

文件7-21的运行结果如图7-23所示：

图7-23文件7-21的运行结果

# ，

过渡页

模块三  任务实现

子任务4：匿名内部类的使用：定义接口Pet，在其中定义抽象方法show()；定义测试类,在其中定义变量name，定义静态方法PetShow(Pet pet)，调用该静态方法，参数为匿名内部类。

模块三 
任务实现

# 任务源码

根据任务描述以及匿名内部类相关知识点可以得到文件7-22所示的代码：

模块三 
任务实现

文件7-22  Example22

1Interface Pet{					//定义接口Pet
2    void show();					//定义抽象方法show()
3}
4public class Example22 {
5	public static void main(String[] args) {
6		 String name = "贝贝";
7	        PetShow(new Pet(){ //调用PetShow()方法，参数为匿名内部类
8	        public void show() {
9	                System.out.println("宠物叫"+name);
10	            }
11	        };
12	    }

13	    public static void PetShow(Pet pet){	//该方法参数为Pet接口类型
14	        pet.show();
15	    }
16	}

# 运行结果

模块三 
任务实现

文件7-22的运行结果如图7-24所示：

图7-24文件7-22的运行结果

# 感谢关注