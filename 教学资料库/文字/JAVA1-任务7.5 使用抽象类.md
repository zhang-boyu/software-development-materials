# 无锡职业技术学院内部资料

JAVA程序设计项目教程

# *

任务描述

知识链接

任务实现

目录

# 子任务2：在子任务1的基础上，定义子类Cat的属性color以及设置和获取方法；定义测试类输出Cat实例相关属性信息。

模块一  任务描述

定义一个抽象类Pet，其中包括成员属性name和抽象方法shout()；定义子类Cat，在Cat类中实现抽象方法shout() ；定义一个测试类以测试抽象类的使用。

# ..

模块二 知识链接

抽象类

抽象方法的定义

抽象类的定义

抽象类的特点

# 抽象类

模块二 
知识链接

定义一个类时，常常需要定义一些成员方法用于描述类的行为特征，但有时这些方法的实现方式是无法确定的。例如，前面定义的Pet类中的shout()方法用于描述动物的叫声，但是不同的动物，叫声也不相同，因此在shout()方法中无法准确描述动物的叫声。
针对上面描述的情况，Java提供了抽象方法来满足这种需求。抽象类有点类似于“模板”的作用，目的是根据其格式来创建和修改新的类，对象不能由抽象类直接创建，只可以通过抽象类派生出新的子类，再由其子类来创建对象。

# 抽象类

模块二 
知识链接

当一个类被声明为抽象类时，要在这个类前面加上修饰符abstract,在抽象类中的成员方法可以包括一般方法和抽象方法。包含了抽象方法的类一律称为抽象类，一个抽象类可以存在一个或多个抽象方法。
抽象方法是只定义了方法的声明但不包含具体实现的方法。抽象方法是使用abstract关键字修饰的成员方法，抽象方法在定义时不需要实现方法体。

# 抽象方法的定义

模块二 
知识链接

抽象方法的语法格式如下：
abstract 返回值类型 方法名称(参数列表); 
例如：abstract void shout();

# 抽象类的定义

模块二 
知识链接

当一个类包含了抽象方法，该类就是抽象类。抽象类和抽象方法一样，必须使用abstract关键字进行修饰。抽象类的语法格式如下:

abstract  class 类名称 {
成员变量;
方法(){……};       //一般方法
abstract 方法()；//抽象方法
          }

例如：
abstract  class Pet {
String name;
void show(){……};       //一般方法
abstract void shout()；//抽象方法
}

# 抽象类的特点

模块二 
知识链接

（1）不能被实例化：抽象类不能直接用来创建对象。
（2）可以包含抽象方法：抽象类中可以包含抽象方法。
（3）可以包含非抽象方法：抽象类中不仅可以包含抽象方法，也可以包含具体实现的方法。

# 抽象类的特点

模块二 
知识链接

（4）可以有构造器：抽象类可以有构造器，用于初始化子类创建对象时需要的一些资源。
（5）子类必须实现所有抽象方法（除非子类也是抽象的）：如果子类不是抽象的，那么它必须实现父类中的所有抽象方法。
（6）抽象类是面向对象编程中一种非常有用的特性，它有助于实现代码的复用和多态性。

# ，

过渡页

模块三  任务实现

定义一个抽象类Pet，其中包括成员属性name和抽象方法shout()；定义子类Cat，在Cat类中实现抽象方法shout() ；定义一个测试类以测试抽象类的使用。

模块三 
任务实现

# 任务源码

结合任务描述和知识链接中抽象类和抽象方法定义知识点可以得到文件7-9所示的代码：

模块三 
任务实现

文件7-9 Example09

//定义抽象类
1public abstract class Pet {
2	String name;
//定义抽象方法shout()
3	abstract void shout();
4}
5public class Cat extends Pet {
//实现抽象方法shout()
6	public void shout() {
7        System.out.println("喵喵……"); 
8	}
9	}

10public class Example09 {
11	public static void main(String[] args) {
12		Cat cat = new Cat();  
13		cat.shout();
14	}
15}

# 运行结果

模块三 
任务实现

文件7-9的运行结果如图7-11所示：

图7-11文件7-9的运行结果

从上图可以看出，控制台打印了“喵喵...”，说明cat对象调用了Cat类中实现的父类Pet的抽象方法shout()。
注意：使用abstract关键字修饰的抽象方法不能使用private关键字修饰，因为抽象方法必须要被子类实现，如果使用了private关键字修饰抽象方法，则子类无法实现该方法。

# 感谢关注