# 无锡职业技术学院内部资料

JAVA程序设计

# *

任务描述

知识链接

任务实现

目录

# *

模块一  任务描述

子任务1 : 定义整数型变量a的值是10，计算b1=a++与b2=++a的值。
子任务2 : 定义整数型变量a的值是1，b的值为2，计算a + b++的值。

# *

模块二 知识链接

算术运算符

# 模块二 
知识链接

算术运算符是最简单、最常用的运算符号。Java中的算术运算符及用法具体如下表所示。

算数运算符及用法

| 运算符 | 运算 | 范例 | 结果 |
|---|---|---|---|
| + | 正号 | +3 | 3 |
| - | 负号 | b=4;-b | -4 |
| + | 加 | 5+5 | 10 |
| - | 减 | 6-4 | 2 |
| * | 乘 | 3*4 | 12 |
| / | 除 | 5/5 | 1 |

# 模块二 
知识链接

算数运算符及用法

| 运算符 | 运算 | 范例 | 结果 |
|---|---|---|---|
| % | 取模（即算术中的求余数） | 7%5 | 2 |
| ++ | 自增（前） | a=2;b=++a; | a=3;b=3; |
| ++ | 自增（后） | a=2;b=a++; | a=3;b=2; |
| -- | 自减（前） | a=2;b=--a; | a=1;b=1; |
| -- | 自减（后） | a=2;b=a--; | a=1;b=2; |

# 算术运算符

模块二 
知识链接

（1）在进行自增（++）和自减（--）运算时，如果运算符++或--放在操作数的前面，则先进行自增或自减运算，再进行其他运算。反之，如果运算符放在操作数的后面，则先进行其他运算再进行自增或自减运算。

# 算术运算符

模块二 
知识链接

（2）在进行除法运算时，当除数和被除数都为整数时，得到的结果也是一个整数。如果除法运算有小数参与，得到的结果会是一个小数。
例如，2510/1000属于整数之间相除，会忽略小数部分，得到的结果是2，而2.5/10的结果为0.25。

# 算术运算符

模块二 
知识链接

（3）在进行取模（%）运算时，运算结果的正负取决于被模数（%左边的数）的符号，与模数（%右边的数）的符号无关。
例如，(-5)%3=-2，而5%(-3)=2。

# *

过渡页

模块三  任务实现

子任务1 : 定义整数型变量a的值是10，计算b1=a++与b2=++a的值。
子任务2 : 定义整数型变量a的值是1，b的值为2，计算a + b++的值。

# 子任务1-代码实现

1  public class Example09 {
2    public static void main(String[] args) {
3		int a=10;
4		int b;
5		System.out.println("a++的值为"+a++);
6		System.out.println("++a的值为"+(++a));
7		b=a++;
8		System.out.println("b=a++的值为"+b);
9		b=++a;
10		System.out.println("b=++a的值为"+b);
11	  }
12  }

模块三 
任务实现

# 运行结果

模块三 
任务实现

# 子任务2-代码实现

1  public class Example10 {
2	  public static void main(String[] args) {
3 		int a = 1;	
4		int b = 2;	
5		int x = a + b++;
6		System.out.println("b="+ b);
7		System.out.println("x="+ x);
8	  }
9  }

模块三 
任务实现

# 运行结果

模块三 
任务实现

# 感谢关注